export type BTAccount = {
    id: string;
    name: string;
    category: string;
    tag: string;
    balance: number;
    availableBalance: number;
};

export type Exchanges = {
    bitcoin: number;
};
